/* eslint-disable no-undef */
import Logger from './logger.mjs'
import EformStarter from './eform-starter.mjs'
import LoadTracker from './load-tracker.mjs'
import CloseTracker from './close-tracker.mjs'

const logger = new Logger('CDInjector-eForm')

/// ///////////////////////////////////////////////////////////////////////////////////////////
/// EFORM STARTER
/// ///////////////////////////////////////////////////////////////////////////////////////////
const eFormStarter = new EformStarter($, logger)
eFormStarter.start()

/// ///////////////////////////////////////////////////////////////////////////////////////////
// POST INCIDENT NUMBER & URL
/// ///////////////////////////////////////////////////////////////////////////////////////////
const loadTracker = new LoadTracker($, logger)
loadTracker.start()

/// ///////////////////////////////////////////////////////////////////////////////////////////
// HOOK INTO CLOSE BUTTON
/// ///////////////////////////////////////////////////////////////////////////////////////////
const closeTracker = new CloseTracker($, logger)
closeTracker.start()
