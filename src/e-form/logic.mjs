export default class {
  constructor (logger) {
    this.logger = logger.extend('logic')
  }

  detectDivision (coordinates) {
    // TODO: Detect the division from the given coordinates
    return ''
  }
}
